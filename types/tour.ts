export interface TourStop {
  id: string;
  placeId: string;
  /** Illustration placeholder — a single emoji, large and centered on the sheet. */
  emoji: string;
  headline: string;
  story: string;
  whyItMatters: string;
  funTip: string;
}
